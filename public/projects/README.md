# Project screenshot slots

Add the supplied screenshots to this folder using these exact names:

- `CapstoneOMS.png`
- `WarehouseInternship.png`
- `CoffeeShop.png`
- `SilverThatch.png`

After the files are added, set `screenshotAvailable` to `true` for the matching records in `components/ProjectsSection/projectsData.js`.

